import React from "react";
// import Welcome from 'We'

const App = () => {
  return (
    
      <>
        {/* ReactDOM.render(<p></p>)
         */}
         ReactDOM.render(<p>Now I can render any React component on any DOM node I want using ReactDOM.render</p>)ReactDOM.render(<p>Now I can render any React component on any DOM node I want using ReactDOM.render</p>)
      </>
  );
};

export default App;
